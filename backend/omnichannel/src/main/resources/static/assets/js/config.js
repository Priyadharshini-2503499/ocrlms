// =====================================================================
// config.js — the ONE place to edit when deploying the frontend.
// =====================================================================

export const CONFIG = {
  // API Gateway address. All paths in endpoints.js are relative to this.
  BASE_URL: "",

  // Currency symbol shown across the UI.
  CURRENCY: "₹",
};

